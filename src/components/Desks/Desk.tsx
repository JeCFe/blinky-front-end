import React from "react";
import "./Bart.css";
import Bart from "./Bart";

const Desk = () => {
  const onClickHandler = () => {};

  return (
    <div onClick={onClickHandler}>
      <Bart />
    </div>
  );
};

export default Desk;
